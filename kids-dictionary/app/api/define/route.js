export async function POST(request) {  const { word } = await request.json();  const cleanWord = word?.trim().toLowerCase();  const prompt = `You are a friendly dictionary for children aged 8-12. The child has looked up the word: "${cleanWord}".Respond with a JSON object in exactly this format:{  "word": "the word exactly as given",  "partOfSpeech": "noun | verb | adjective | adverb | etc.",  "definition": "A clear, simple definition using easy words that an 8-12 year old can understand. 1-2 sentences.",  "examples": [    "A fun, relatable example sentence using the word in context.",    "A second fun, relatable example sentence using the word in context."  ],  "funFact": "One short, interesting or surprising fact related to the word."}Use simple, friendly language. Make examples relevant to a child's life. If the word doesn't exist, set definition to "Hmm, we couldn't find that word! Try checking the spelling." Respond with ONLY the JSON object, no extra text.`;  try {    const apiKey = process.env.GOOGLE_API_KEY;    if (!apiKey) return Response.json({ error: "No API key configured" }, { status: 500 });    const res = await fetch(      `https://generativelanguage.googleapis.com/v1/models/gemini-1.5-flash:generateContent?key=${apiKey}`,      {        method: "POST",        headers: { "Content-Type": "application/json" },        body: JSON.stringify({ contents: [{ parts: [{ text: prompt }] }] }),      }    );    if (!res.ok) {      const errText = await res.text();      return Response.json({ error: "API error: " + res.status + " " + errText.slice(0, 200) }, { status: 500 });
